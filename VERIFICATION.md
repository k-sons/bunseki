# ✅ 실제 앱 동작 검증 기록

> 검증일: 2026-08-24 · `146 pass / 0 fail`
> **테스트가 통과했다는 것과 앱이 실제로 쓸 만하다는 것은 다른 이야기**라, 브라우저에서 직접 눌러 확인한 기록입니다.

---

## 0. 한 줄 결론

**앱을 쓰는 데 추가 코드 수정은 필요하지 않습니다.** 5개 탭 전부 정상 동작하고,
전 과정에서 **콘솔 오류 0건 · 가로 넘침 0px** 입니다.

---

## 1. 왜 따로 확인했나

이 저장소의 검증은 세 층입니다.

| 층 | 무엇을 보나 | 한계 |
| :--- | :--- | :--- |
| `npm test` (146개) | 엔진이 내놓는 **데이터**가 맞는가 | 화면에 어떻게 그려지는지는 모름 |
| `test/browser-verify.html` | 렌더러가 **그리는 결과**가 맞는가 | 하네스라서 **앱 자체는 아님** |
| **이 문서** | **앱을 실제로 눌러서** 쓸 수 있는가 | — |

세 번째가 비어 있었습니다. 하네스는 렌더러 함수를 직접 불러 쓰는 페이지라,
`index.html` + `main.js` 로 조립된 **진짜 앱**이 도는지는 확인된 적이 없었습니다.

---

## 2. 어떻게 확인했나

`npx vite --port 5199` 로 개발 서버를 띄우고, **headless Chrome 을 원격 디버깅(CDP)으로 조종**해
사람이 하듯 버튼을 눌렀습니다.

```bash
npx vite --port 5199 &
"/c/Program Files/Google/Chrome/Application/chrome.exe" \
  --headless=new --disable-gpu --remote-debugging-port=9222 \
  --user-data-dir=<임시> --window-size=1400,1000 "http://localhost:5199/"
# 이후 Node 에서 ws://…/devtools/page/… 에 붙어 Runtime.evaluate 로 클릭·조회
```

> **참고**: Chrome 확장(claude-in-chrome)으로는 이 개발 서버에 닿지 않았습니다.
> 셸이 샌드박스 안에서 돌아 **바깥 브라우저와 네트워크가 분리**돼 있기 때문입니다.
> 같은 샌드박스 안에서 뜨는 headless Chrome 은 정상적으로 접속됩니다.

검증 스크립트는 저장소 밖(세션 scratchpad)에 두었습니다 — 작업트리는 깨끗하게 유지합니다.

---

## 3. 확인 결과

### 3-1. 빌드

```
dist/index.html                    11.10 kB │ gzip:  3.47 kB
dist/assets/index-*.js             60.12 kB │ gzip: 20.13 kB   ← 초기 번들
dist/assets/lib-*.js              300.55 kB │ gzip: 78.94 kB   ← @babel/parser, 지연 로드
✓ built in 983ms
```

**babel 이 초기 번들에서 빠져 있는 설계가 그대로 유지**되고 있습니다 —
`dist/index.html` 에 `lib` 의 `modulepreload` 가 **없음**을 확인했습니다.

### 3-2. 앱 동작

| 확인 항목 | 결과 |
| :--- | :--- |
| 앱 로드 · 탭 5개 렌더 | ✅ `하이라이트 / 구조맵 / 메트릭 / 플로우 / 동작` |
| 내장 예제 버튼 → 분석 | ✅ 정상 |
| **직접 붙여넣기** (101줄 실제형 파일) | ✅ 5개 탭 전부 렌더 |
| TypeScript `.tsx` (제네릭 · `memo` · 타입 주석) | ✅ `위험 2 · 경합 1` 정상 표시 |
| 대용량 (2577줄 · 26 컴포넌트 · Hook 572개) | ✅ 정상, 1초 미만 |
| 콘솔 오류 / 경고 | ✅ **전 과정 0건** |
| 가로 넘침 | ✅ **전 과정 0px** |
| 상태바 집계 | ✅ `줄 / 함수 / 컴포넌트 / Hook / Import / 언어` |

### 3-3. 오탐 점검

**제대로 쓴 코드**(`AbortController` · `alive` 가드 · `useMemo`/`useCallback` · 커스텀 훅 3개)를
넣었을 때 **위험 배지 0 · deps 빠짐 0 · 루프/경합 0** 이었습니다.
경고가 하나라도 떴다면 그게 오탐입니다 — 없었습니다.

반대로 가드가 없는 `.tsx` 파일에서는 `위험 2 · 경합 1` 이 제대로 떴습니다.

### 3-4. 성능

| 코드 크기 | 동작 엔진 | 구조 파서 |
| ---: | ---: | ---: |
| 795줄 | 30ms | 31ms |
| 1587줄 | 46ms | 53ms |
| 3171줄 | 88ms | 52ms |

대략 **선형**입니다. 붙여넣고 기다리는 느낌은 없습니다.

### 3-5. 이번 라운드 기능이 실제 앱에 나오는지

내장 예제의 ⚡ 동작 탭에서 확인됐습니다:

```
<SearchBar onSearch> → handleSearch() → ⏳ 응답 대기 → dispatch() → 리렌더
```

이 `⏳ 응답 대기` 자리가 이번에 생긴 것입니다.
전에는 **핸들러 안에서 기다리는 구간이 어디에도 안 나와** 누르자마자 다 끝나는 것처럼 보였습니다.

---

### 3-6. 문법이 깨진 코드 (수정 후 재확인)

문서를 검증하다 **실제 문제**를 찾아 고쳤습니다.

**발견** — `91f1a13` 에서 `parser.js` 가 AST 로 바뀌면서 잘린 코드 내성이 사라졌는데,
실패 사실이 어디에도 실려 있지 않았습니다. 그 결과 구조맵은 **빈 화면**, 메트릭은
**전부 `0`**, 상태바도 **전부 `0`** — **"세어 보니 없다" 와 "못 셌다" 가 구분되지 않았습니다.**
게다가 동작 탭 오류 문구가 *"다른 탭은 그대로 사용할 수 있습니다"* 라고 **거짓말**을 했습니다.

**수정** (`ParseResult.error` 추가) 후 실측:

| 탭 | 잘린 코드에서 보이는 것 |
| :--- | :--- |
| 하이라이트 | 안내 카드 **+ 코드 자체** (안내 1개 · 코드 12줄 확인) |
| 구조맵 / 메트릭 / 플로우 | 안내 카드 (`문법 오류로 코드를 읽지 못했습니다` · `L11`) |
| 동작 | 안내 카드 (문구도 사실에 맞게 수정) |
| 상태바 | `함수: —` 처럼 **줄표** |

- 오류 메시지 **HTML 주입 0건** (`textContent` 로 넣음)
- **360px 가로 넘침 0px** · 콘솔 오류 0건
- **온전한 코드는 회귀 없음** — 다섯 탭 렌더 결과 글자 수가 수정 전과 동일

---

## 4. 버그는 아니지만 알아 둘 것

- **2577줄짜리를 넣으면 동작 탭 하나가 약 2만 자**가 됩니다. 오류도 느려짐도 없지만
  스크롤이 아주 깁니다. 접기·컴포넌트 고르기 같은 **탐색 수단**이 필요한지는
  실제로 써 보고 판단하기로 했습니다 (`HANDOFF.md` 6장 후보 (D)).
- `npx vite` 는 기본적으로 **IPv6 `localhost` 에만 바인딩**합니다.
  다른 기기나 다른 네트워크 경로에서 열어야 하면 `--host 127.0.0.1` (또는 `--host`) 를 주세요.

---

## 5. 재현 방법

```bash
npm install
npm test          # 146 pass / 0 fail
npx vite build    # 프로덕션 빌드
npm run dev       # http://localhost:5173 에서 직접 확인
```
