# 📘 Code Bunseki — 개발자/실무자용 메트릭 & 아키텍처 가이드 (Pro Version)

> **Code Bunseki**는 React / React Native 및 JSX/TSX 코드의 구문, 상태 관리 패턴, 훅(Hook) 사용 비중, 컴포넌트 간 호출 지도, 그리고 **이벤트에서 시작하는 상태 변화 연쇄**를 시각화하여 **코드 구조와 런타임 동작을 함께 파악하는 개발자 도구**입니다.

---

## 📑 목차
1. [핵심 아키텍처 — 두 개의 분석 엔진](#1-핵심-아키텍처--두-개의-분석-엔진)
2. [분석 출력 대시보드 5가지 탭 기술 명세](#2-분석-출력-대시보드-5가지-탭-기술-명세)
   - [① 하이라이트 (Highlight View)](#-하이라이트-highlight-view)
   - [② 구조맵 (Structure Map View)](#-구조맵-structure-map-view)
   - [③ 메트릭 대시보드 (Metrics Dashboard)](#-메트릭-대시보드-metrics-dashboard)
   - [④ 컴포넌트 플로우 (Flow Chart)](#-컴포넌트-플로우-flow-chart)
   - [⑤ 동작 분석 (Behavior View)](#-동작-분석-behavior-view)
3. [동작 분석 상세 — 상태 변화 연쇄 추적](#3-동작-분석-상세--상태-변화-연쇄-추적)
   - [3-7. 비동기 타이밍과 언마운트 위험](#3-7-비동기-타이밍과-언마운트-위험-timingjs)
   - [3-8. deps 점검 — stale closure](#3-8-deps-점검--stale-closure-depsjs)
   - [3-9. Effect 사이 관계](#3-9-effect-사이-관계-interplayjs)
   - [3-10. 조건부 실행 — 관문(gate)](#3-10-조건부-실행--관문gate)
   - [3-11. 이벤트 핸들러 안의 비동기](#3-11-이벤트-핸들러-안의-비동기)
4. [스코프 경계(Boundary) — 단일 파일 분석의 한계 표기](#4-스코프-경계boundary--단일-파일-분석의-한계-표기)
5. [정량적 메트릭 수치를 활용한 코드 리팩토링 전략](#5-정량적-메트릭-수치를-활용한-코드-리팩토링-전략)
6. [조각/부분 코드(Truncated Code) 분석 매커니즘](#6-조각부분-코드truncated-code-분석-매커니즘)
7. [도구가 보장하는 것과 보장하지 않는 것](#7-도구가-보장하는-것과-보장하지-않는-것)
8. [협업 및 문서화 (Export & Markdown Report)](#8-협업-및-문서화-export--markdown-report)
9. [검증 체계](#9-검증-체계)

---

## 1. 핵심 아키텍처 — 두 개의 분석 엔진

Code Bunseki 는 목적이 다른 **두 개의 분석기**를 병행 운용합니다.
**둘 다 `@babel/parser` AST 기반**이며, 나누어 둔 이유는 파싱 방식이 아니라 **묻는 질문이 다르기** 때문입니다.

```
                      코드 입력
                          │
        ┌─────────────────┴──────────────────┐
        ▼                                    ▼
  구조 분석기                            동작 분석기
  src/core/parser.js                    src/core/behavior/
  "무엇이 있는가"                        "어떻게 동작하는가"
        │                                    │
  ┌─────┼─────┬───────┐                      ▼
  ▼     ▼     ▼       ▼                 ⑤ 동작
① 하이라이트 ② 구조맵 ③ 메트릭 ④ 플로우

  ※ 둘 다 @babel/parser 를 쓰며 같은 `lib` 청크를 공유합니다 (지연 로드)
```

> **📌 이력**: `parser.js` 는 원래 **줄 단위 정규식 + 중괄호 깊이** 방식이었습니다.
> 구조분해 props `{ }`, `memo`/`forwardRef` 래퍼, TS 제네릭, 여러 줄 시그니처에서
> 함수를 놓치거나 줄 수를 1 로 잘못 세는 문제가 있어 **커밋 `91f1a13` 에서 AST 로 전면
> 재작성**했습니다. 출력 형태(`ParseResult`)는 그대로 유지해 렌더러 4개는 수정하지 않았습니다.
> **아래 설명은 재작성 이후 기준입니다.**

### 1-1. 구조 분석기 (`src/core/parser.js`)

AST 에서 **선언과 위치**를 뽑아 `ParseResult` 로 내놓습니다.

- **Import / Export**: ESM 의존성 및 Default/Named Export 추적
- **컴포넌트 & 함수 식별**: 대문자 시작 최상위 선언 + **JSX 를 담은 중첩 선언**.
  `memo(forwardRef(fn))` 같은 래퍼는 벗겨서 실제 함수 노드를 잡습니다.
- **Hook 식별 및 분류**: `state` / `effect` / `memo` / `store` / `handler` / `rn` / `async`
- **정확한 라인 범위**: AST 노드의 `loc` 을 쓰므로 여러 줄 시그니처도 실제 범위로 셉니다

**한계**: 선언의 존재는 알지만 **의미 관계는 얕게만** 봅니다. `setOpen` 이 `open` 의 setter 라는 사실,
`onClick` 이 어떤 함수에 바인딩됐는지, `useEffect` 의 deps 안에 무엇이 있고 그것이 언제 재실행을
부르는지는 동작 분석기의 몫입니다.

### 1-2. 동작 분석기 (`src/core/behavior/`)

| 모듈 | 역할 |
| :--- | :--- |
| `index.js` | 진입점 `parseBehavior(code)`, 파싱 오류 한국어 변환 |
| `collect.js` | AST 순회 — 상태·Effect·핸들러·로컬 함수 수집 (공용 `walk` 헬퍼) |
| `hooks.js` | 커스텀 훅 분석, 안팎 이름 대응, 훅이 돌려준 콜백, 스코프 경계 |
| `chain.js` | 이벤트 연쇄 조립 → 평탄화된 `Step[]` (대기·관문·훅 구역 포함) |
| `timing.js` | 비동기 타이밍·언마운트 위험·참조 수집·대기 무게·관문 판정 |
| `deps.js` | deps 에서 빠진 값 (stale closure) |
| `interplay.js` | Effect 사이 관계 — 무한 루프 / 경합 / 연쇄 |

**번들 전략**: `@babel/parser` 는 두 분석기 **양쪽에서만** import 되고 `main.js` 는 둘 다
**동적 `import()`** 로 부릅니다. 그래서 babel 이 초기 번들에서 빠지고 공용 `lib` 청크로 분리됩니다.

```
dist/assets/index-*.js       59.10 kB │ gzip: 19.82 kB   ← 초기 로딩 (파서 미포함)
dist/assets/parser-*.js       9.61 kB │ gzip:  3.33 kB   ← 구조 분석기
dist/assets/behavior-*.js    31.87 kB │ gzip: 10.85 kB   ← 동작 분석기
dist/assets/lib-*.js        300.55 kB │ gzip: 78.94 kB   ← @babel/parser (첫 분석 시)
```

> ⚠️ **두 분석기는 같은 파싱 실패를 공유합니다.** 예전 정규식 파서는 잘린 코드에서도 버텼지만,
> 지금은 **문법이 깨지면 ①~④ 탭도 빈 결과**가 됩니다. 자세한 내용은 [6장](#6-조각부분-코드truncated-code-분석-매커니즘) 참고.

---

## 2. 분석 출력 대시보드 5가지 탭 기술 명세

### 🎨 ① 하이라이트 (Highlight View)

Prism.js 구문 강조 위에 섹션 컬러밴드와 인라인 배지를 주입합니다.

**섹션 밴드** (`buildSectionMap` 이 각 라인에 할당)
`IMPORT` · `CONST` · `COMPONENT` · `HELPER` · `EXPORT`

**인라인 배지** (Hook 카테고리별 색상 구분)

| 카테고리 | 대상 |
| :--- | :--- |
| `state` | `useState`, `useReducer` |
| `effect` | `useEffect`, `useLayoutEffect`, `useInsertionEffect` |
| `memo` | `useMemo`, `useCallback`, `useDeferredValue`, `memo` |
| `store` | `useSelector`, `useDispatch`, `useStore`, `useContext`, `dispatch` |
| `handler` | `onClick`, `onChange`, `onPress` 등 `on[A-Z]` 패턴 |
| `rn` | `StyleSheet.create`, `Animated.*` |
| `async` | `async`, `await` |

**색상 배치**: 배지는 한 줄에 여러 개가 나란히 붙으므로 색상환에서 서로 20도 이상 떨어뜨렸습니다.

```
 35°  memo      90%     150°  state    75%     175°  rn       65%
210°  import    15% (중립)   220°  effect   85%     255°  async    75%
280°  store     70%     330°  handler  75%
```

`handler` 와 `memo` 는 `const handleX = useCallback(...)` 처럼 동일 라인에 공존하는 빈도가 높아 대척점(330° / 35°)에 배치했습니다. `import`(React 패키지 표시)는 채도 15% 의 중립색이라 인접 색상값과 충돌하지 않습니다.

> **구현 노트**: Prism 출력을 라인 단위로 자를 때 열린 태그를 스택으로 추적해 줄 끝에서 닫고 다음 줄에서 다시 엽니다. 단순 `split('\n')` 을 쓰면 블록 주석·템플릿 리터럴처럼 여러 줄에 걸친 토큰의 `<span>` 이 잘려 색상이 유실됩니다.

---

### 🗺️ ② 구조맵 (Structure Map View)

파일 내부 구성 요소를 계층적 카드로 구조화합니다.

- **라인 범위 표기**: `L11–L33` 형식으로 시작–끝 범위 명시
- **Hook 감지 위치 나열**: 2회 이상 사용된 Hook 의 모든 라인 번호(`L25, L175`) 명시
- **복잡도 경고**: 라인 수 > 100 또는 Hook > 5개 → `🚨 Refactor` 배지
- **인터랙티브 스크롤**: 카드 클릭 시 하이라이트 뷰의 해당 라인으로 이동

---

### 📊 ③ 메트릭 대시보드 (Metrics Dashboard)

- **Summary Cards**: 총 라인 수, 컴포넌트 수, 함수 수, Hook 수, Import 수, 상수 수, JSX 요소 수, 주석 수
- **Function Size Bar Chart**: 라인 수 내림차순 정렬, `L11–L33` 범위 표시, 클릭 시 이동
- **Hook Breakdown Chart**: 사용 빈도 시각화, 호버 시 감지 라인 툴팁, 클릭 시 이동
- **이중 검증 (Traceability)**: 모든 수치가 소스 라인 근거를 함께 제시하여 즉시 상호 검증 가능

---

### 🔄 ④ 컴포넌트 플로우 (Flow Chart)

컴포넌트 간 렌더링 관계 및 함수 호출 흐름을 DAG 로 표현합니다.

- 🟣 **Component Node**: React 컴포넌트 (사용 Hook 배지 부착)
- 🟡 **Function Node**: 헬퍼/유틸리티 함수 (라인 수, `Async` 배지 부착)

**엣지 관계**

| 표기 | 의미 |
| :--- | :--- |
| `renders` (실선) | 부모가 하위 컴포넌트를 JSX 에서 인스턴스화 |
| `calls` (점선) | 스코프 내부에서 헬퍼 함수를 동기 호출 |
| `calls Async` (하늘색 + 배지) | `await` / `fetch` / `.then` 을 포함하는 비동기 호출 |

**독해 요령**
1. **단방향 의존성 검증** — 상위 노드에서 하위로 결합도가 수직 하강하는지 확인
2. **비동기 병목 추적** — `calls Async` 라인으로 네트워크·타이머 대기 지점 식별
3. **High Fan-out 식별** — 화살표가 과도하게 뻗는 노드는 SRP 위반 후보

> **📌 옛 문서 정정**: 예전에는 "주석이나 문자열 안의 함수명도 호출로 집계될 수 있다" 고 적혀
> 있었습니다. 정규식 파서 시절 이야기이고, **지금은 AST 기반이라 주석·문자열 속 이름은
> 집계되지 않습니다.**
>
> 다만 이 탭은 여전히 **호출의 존재**만 봅니다. "언제·몇 번·어떤 조건에서 불리는가" 는
> 동작 탭(⑤)의 몫입니다.

---

### ⚡ ⑤ 동작 분석 (Behavior View)

앞의 네 탭이 **"무엇이 있는가"** 를 다룬다면, 이 탭은 **"어떻게 동작하는가"** 를 다룹니다.

**UI 구성** — 이 탭은 **세 덩어리**입니다.

1. **이벤트 연쇄** — 상단에 핸들러 칩(`<button onClick> L13`), 고르면 아래에 번호 매긴 세로 스텝
2. **⏱ 타이밍 · deps 점검** — 가로 타임라인 (비동기이거나 deps 가 빠진 Effect 만)
3. **🔗 Effect 사이 관계** — 무한 루프 / 경합 / 연쇄 카드

**연쇄 스텝 종류**

| `kind` | 표시 | 색 | 번호 | 클릭 이동 |
| :--- | :--- | :--- | :--- | :--- |
| `event` | 이벤트 | 🔵 파랑 200° | ○ | JSX 속성 위치 |
| `setter` | 상태 변경 | 🟣 보라 280° | ○ | 호출 위치 |
| `effect` | Effect 실행 | 🟡 노랑 45° | ○ | `useEffect` 선언 위치 |
| `call` | 함수 호출 | 🟢 초록 160° | ○ | 로컬 함수는 선언 위치, Effect 내부 호출은 호출 위치 |
| `wait` | ⏳ 응답 대기 | 줄무늬 | **✕** | — |
| `gate` | ↩ 관문 | 끊긴 테두리 | **✕** | 조건문 위치 |
| `rerender` | 화면 갱신 | ⚪ 회색 | ○ | — |
| `boundary` | 범위 밖 | 🟠 주황 30° 점선 | **✕** (`·`) | — |

`wait`·`gate`·`boundary` 가 번호를 먹지 않으므로 **① ② ③ 번호가 건너뛰지 않습니다.**

**훅 구역(hook zone)** — 이어지는 스텝이 같은 커스텀 훅에 속하면 **점선 상자**로 묶습니다
(청록 175°, 다른 스텝 색과 겹치지 않게). 머리말 클릭 = 훅 선언으로 이동.

> ⚠️ 여기서 말하는 "훅 안" 은 **코드가 적힌 자리가 아니라 상태·Effect 가 사는 자리**입니다.
> `setQuery()` 는 컴포넌트에서 부르지만 그 상태가 훅 것이라면 구역 안에 놓입니다.

---

## 3. 동작 분석 상세 — 상태 변화 연쇄 추적

### 3-1. 추적하는 연쇄

```
이벤트 → setter 호출 → 상태 변경 → deps 가 일치하는 Effect 재실행
       → 내부 호출 / 비동기 → 또 다른 setter → 리렌더
```

실제 출력 예시:

```
① 이벤트         <button onClick>              L13
② 상태 변경      setOpen()  →  open            L13
   └ deps [open] 에 'open' 가 있어 다시 실행됩니다
③ Effect 실행    useEffect 재실행   [.then]     L8
④ 함수 호출      fetchData()   [비동기]         L10
⑤ 상태 변경      setData()  →  data
⑥ 화면 갱신      리렌더
```

### 3-2. 상태로 인식하는 대상

| 선언 | `kind` | 비고 |
| :--- | :--- | :--- |
| `const [x, setX] = useState()` | `state` | ArrayPattern 구조분해로 결합 |
| `const [s, dispatch] = useReducer()` | `reducer` | 동일 구조로 처리 |
| `const dispatch = useDispatch()` | `store` | 대상이 컴포넌트 밖 → Effect 연결 미판정 |

`store` 는 상태가 외부에 있으므로 로컬 Effect 연결을 따지지 않고 *"외부 스토어가 바뀌어 구독 중인 컴포넌트들이 다시 그려집니다"* 로 종료합니다.

> ⚠️ **`store` setter 는 언마운트 위험 판정에서도 제외**합니다. `dispatch()` 는 **없는 컴포넌트에
> 상태를 쓰는 것이 아니라 살아 있는 스토어로 보내는 것**이라 React 가 경고하지 않습니다.
> (흐름에는 그대로 그립니다 — 빼는 것은 위험 판정뿐입니다.)

### 3-3. 핸들러 해석 규칙

| 패턴 | 처리 |
| :--- | :--- |
| `onClick={() => setOpen(true)}` | 인라인 표현식에서 setter 직접 탐지 |
| `onChange={setQuery}` | setter 를 값으로 전달 — 참조 자체를 setter 호출로 간주 |
| `onClick={handleClick}` | 로컬 함수로 진입해 내부 setter 탐지 |
| `onClick={reset}` → `reset()` → `clearAll()` | 최대 3단계까지 간접 호출 추적 |
| `.then(setData)` | 인자로 넘긴 setter 참조도 호출로 간주 |

**함수 래퍼 해제**: `memo` / `forwardRef` / `useCallback` / `observer` 로 감싼 선언은 래퍼를 벗겨 실제 함수 노드를 추출합니다. `memo(forwardRef(fn))` 처럼 중첩된 경우도 처리합니다.

**오탐 방지**
- 로컬 함수 추적 시 `obj.foo()` 형태(MemberExpression)는 따라가지 않습니다. `new Set().add(id)` 의 `add` 가 동명의 로컬 함수 `add()` 로 오인되는 문제가 실제로 발생했습니다.
- 해당 범위 안에서 선언된 함수는 호출 목록에서 제외합니다. 본문도 함께 순회하므로 `load()` 와 그 안의 `fetchUser()` 가 중복 노출됩니다.
- 배열 / Set / Map / 문자열 내장 메서드(`map`, `filter`, `has`, `then` 등)는 흐름 설명이 아니라 노이즈이므로 제외합니다.

### 3-4. 컴포넌트 탐색

- **최상위**: 대문자로 시작하는 함수 선언 및 변수 할당 (기존 규칙과 동일)
- **중첩**: 다른 함수 내부에 선언된 것은 **대문자 + JSX 포함** 조건을 추가로 확인합니다. 이름만 보면 대문자로 시작하는 일반 헬퍼까지 컴포넌트로 오인되기 때문입니다.
- 수집 시 다른 컴포넌트의 서브트리를 가지치기하여, 중첩 컴포넌트의 상태가 바깥 컴포넌트에 귀속되지 않도록 합니다.

### 3-5. 커스텀 훅 추적

로직을 커스텀 훅으로 추출하는 것은 권장 패턴이므로, **잘 분리된 코드일수록 상태와 Effect 가 컴포넌트가 아니라 훅 안에 있습니다.** 훅을 보지 못하면 정작 잘 만든 코드에서 아무것도 찾지 못합니다.

**해석 절차**
1. `use[A-Z]` 로 시작하는 최상위 함수를 훅으로 식별 (React 내장 훅 제외)
2. 훅의 `return` 문을 읽어 **내보낸 이름 → 내부 이름** 대응표 생성
   - `return { query, setQuery }` (ObjectExpression)
   - `return [query, setQuery]` (ArrayExpression)
3. 호출부 구조분해를 대응표로 해석
   - `const { setQuery } = useSearch()`
   - `const { setQuery: setQ } = useSearch()` — 이름 변경도 연결
4. 훅의 Effect 와 **훅 내부에서만 쓰는 setter** 도 함께 병합

4번이 필요한 이유: 컴포넌트가 `setUser` 를 구조분해하지 않더라도, 훅의 Effect 안에서 일어나는 `setUser()` 호출을 연쇄에 표시해야 하기 때문입니다.

**출력 표현** — 초기에는 상태마다 `[useSearch]` 배지와 *"useSearch 훅이 관리하는 상태입니다"*
주석을 붙였지만, 연쇄를 죽 늘어놓기만 하면 **컴포넌트 상태와 훅 상태가 같은 자리에 있는 것처럼**
보였습니다. 지금은 **훅 구역(점선 상자)** 으로 묶고 배지·주석은 뺐습니다(같은 말의 중복).

```
 ① <input onChange>
 ┌ 🪝 useSearch — 이 구간의 상태와 Effect 는 이 훅 안에 있습니다  L3 ┐
 │ ② setQuery() → query   ③ useEffect 재실행   ④ search()          │
 │ ⑤ ⏳ 응답 대기          ⑥ setHits() → hits                       │
 └───────────────────────────────────────────────────────────────┘
 ⑦ 리렌더                       ← 구역이 닫히는 자리 = 흐름이 훅에서 나오는 자리
```

이름을 바꿔 받은 경우 양쪽 이름을 함께 적습니다 —
`toggle() → on (useToggle 안에서는 setOn() 입니다)`.

### 3-6. 훅이 내보낸 핸들러

커스텀 훅은 상태만 돌려주지 않습니다. `const { onSelect } = useSelection()` 처럼
**콜백을 돌려주고 그대로 `onClick` 에 꽂는** 형태가 흔합니다.

- `analyzeHook` 이 훅의 로컬 함수(`localFns`)도 함께 내보내고, 내보낸 이름이 setter 가
  아니면 훅 안의 함수인지 확인합니다.
- `return { inc: () => setN(n+1) }` 처럼 **그 자리에서 만들어 돌려주는 콜백**도 노드째로 잡습니다.
- 이름이 겹치면 **컴포넌트 자신의 함수가 이깁니다.**
- 결과적으로 `call` 스텝에서 **훅 구역이 열리고** 그 뒤 setter·Effect 까지 끊기지 않습니다.

**import 한 훅이 돌려준 콜백은 그대로 경계**입니다 — 안을 못 보는데 상자를 치면
"따라가 봤다" 는 거짓말이 됩니다.

---

### 3-7. 비동기 타이밍과 언마운트 위험 (`timing.js`)

Effect 마다 다음을 판정합니다.

| 필드 | 뜻 |
| :--- | :--- |
| `isAsync` / `asyncKind` | `await` · `.then`/`.catch`/`.finally` · `fetch` 중 무엇으로 기다리는가 |
| `setters[].deferred` | **응답 이후**에 실행되는 setState 인가 |
| `setters[].guarded` | 그 setState 가 가드 안에 있는가 |
| `setters[].onError` | **에러일 때만** 불리는 자리인가 (`.catch` · `try/catch` · `.then(ok, err)` 둘째 인자) |
| `hasCleanup` / `hasAbort` | 정리 함수 / `AbortController` |
| `risk` | `비동기 && 가드 없는 deferred setState` → `unmount-setstate` |

**⚠️ 가드와 관문의 경계 (핵심)**

이른 반환은 **응답을 기다린 뒤**에 있을 때만 언마운트 가드입니다.

```jsx
useEffect(() => {
  if (!id) return                 // ← 관문(gate). 실행을 막을 뿐,
  fetchUser(id).then(setUser)     //   "응답이 온 시점에 살아 있는가" 를 묻지 않는다
}, [id])                          //   → 위험 경고는 그대로 뜬다

useEffect(() => {
  let alive = true
  fetchUser(id).then((u) => {
    if (!alive) return            // ← 이건 진짜 언마운트 가드
    setUser(u)
  })
  return () => { alive = false }
}, [id])
```

`findGates()` 가 `await` 에서 훑기를 멈추는 것과 **같은 경계**입니다.
한쪽 규칙을 넓히면 다른 쪽도 반드시 함께 확인해야 합니다.

> 한때 앞쪽 형태에서도 뒤따르는 setState 를 전부 가드로 세어 **위험 경고가 통째로 사라지고**
> 있었습니다. 가장 흔한 형태였던 만큼 영향이 컸습니다(커밋 `de1fd8d` 에서 수정).

**상대 시간감** — `async-wait` 스텝은 `weight`(2~12) 를 가집니다.
`await` 하는 **식 안에서만** 지연 리터럴을 찾습니다(`await sleep(2000)`,
`await new Promise(r => setTimeout(r, 300))`). Effect 어딘가의 `setInterval(…, 1000)` 을
응답 대기 시간으로 오해하지 않기 위해서입니다. 못 찾으면 `시간 미상`(무게 6).

### 3-8. deps 점검 — stale closure (`deps.js`)

Effect 가 **읽는** 이름과 `deps` 를 대조해 빠진 값을 찾습니다.
**헛경보를 안 내는 것이 이 검사의 전부**라, 다음은 세지 않습니다.

- 모듈/전역 이름 · setter · `useRef`/`useDispatch` 결과 · `const` 리터럴 상수
- Effect 안에서 선언한 이름(가림 포함) · `user.id` 의 속성 이름 · 중첩 함수 파라미터
- `catch (err)` 처럼 **그 자리에서 새로 묶는 이름**
- **`deps` 배열이 아예 없는 Effect** (매 렌더 새로 만들어져 옛 값을 붙잡을 일이 없음)

`[props.id]` 처럼 식으로 쓴 deps 도 **뿌리 이름**(`props`)으로 인정합니다.
실무 코드에서 ESLint `exhaustive-deps` 와 같은 지점을 가리키는지 확인했습니다.

### 3-9. Effect 사이 관계 (`interplay.js`)

`timing` 결과만 재료로 써서, "쓴다"(setter) 와 "읽는다"(`deps` 에 그 상태가 있음) 를
간선으로 잇는 그래프를 만듭니다.

| 종류 | 판정 | 심각도 |
| :--- | :--- | :--- |
| **무한 루프** | 고리. 스스로 되받거나 둘이 서로 되받음. **deps 배열이 없는 Effect** 가 상태를 바꾸는 것도 포함 | setter 가 전부 조건문 안이면 `warn`, 아니면 `risk` |
| **경합** | 같은 상태를 Effect 둘 이상이 바꿈 | 하나라도 비동기면 `risk`(늦게 온 옛 응답이 새 값을 덮음), 전부 동기면 `info` |
| **연쇄** | 고리가 아닌 간선을 최대 4단계까지 한 줄로 | `info` |

고리에 속한 간선은 연쇄로 중복 보고하지 않습니다. 항목은 심각도순, 컴포넌트당 최대 8개.

**오탐 방지**: setter 마다 `nested`(중첩 함수 안에서 불리는가)를 봅니다.
`setInterval(() => setX())` 처럼 **나중에** 불리는 setter 를 매 렌더 루프로 오탐하지
않으려는 것으로, 매 렌더 루프는 `!nested && !deferred` 인 **곧바로 부르는 setter** 만 셉니다.

### 3-10. 조건부 실행 — 관문(gate)

타임라인이 알약을 죽 늘어놓기만 하면 첫 줄에서 되돌아 나가는 Effect 도
**"언제나 끝까지 간다" 처럼** 보입니다. `findGates()` 가 이른 반환을 모읍니다.

- 조건식은 **원본 소스를 그대로 잘라** 씁니다(공백 정리 + 32자). 그래서
  `analyzeEffectsTiming(effects, states, **code**)` · `buildEvents(name, collected, scope, **code**)`
  로 원본이 흘러갑니다.
- **곧바로 부르는 함수 한 겹까지** 들어갑니다 — `(async () => {…})()` IIFE, 최상위에
  선언하고 최상위에서 부르는 `load()`.
- 블록에 문장이 몇 개든 **마지막이 `return`/`throw`** 면 이른 반환입니다
  (`if (!id) { reset(); return }`). 앞 문장들은 나가는 길에 하는 일일 뿐입니다.

**관문으로 세지 않는 것** (헛경보 방지)

| 형태 | 이유 |
| :--- | :--- |
| `await` 뒤의 이른 반환 | 실행을 막는 관문이 아니라 언마운트 가드 (이미 `🛡 가드됨` 으로 보임) |
| 나중에 불릴 콜백 안 | `setInterval(() => { if (!on) return … })` 은 Effect 가 도는 그 순간의 관문이 아님 |
| `else` 가 붙은 `if` | 갈림길일 뿐 "여기서 끝" 이 아님 |

⏱ 타임라인은 관문과 즉시 setter 를 **줄 번호로 세워** 코드 순서대로 놓고,
⚡ 연쇄는 **Effect(또는 호출) 스텝 바로 뒤에 모읍니다** — 말하려는 건
"아래 단계로 못 갈 수 있다" 하나뿐이라 그것으로 충분합니다.
**세 자리 모두 `describeGate()` 가 만든 같은 문장**을 씁니다.

### 3-11. 이벤트 핸들러 안의 비동기

⏱ 섹션은 Effect 만 봅니다. 그래서
`onClick={async () => { … await … setX() }}` 의 기다리는 구간이 **어디에도 나오지 않아**
누르자마자 다 끝나는 것처럼 보였습니다.

- `resolveHandlerSetters` 가 **실제로 실행되는 본문들**(인라인 화살표 + 거쳐 가는 로컬 함수)을
  함께 내보내고, `describeHandlerAsync` 가 그것을 합칩니다.
- 한 곳이라도 기다리면 기다리는 것이고, 대기 무게는 **가장 오래 기다리는 쪽**입니다.
- 연쇄는 **이름 단위**라, 한 곳에서라도 응답 전에 불리는 이름은 **응답 전**으로 봅니다
  (`setLoading(true) … setLoading(false)`).
- `collectHandlerGates` 로 **핸들러의 관문**도 같은 규칙으로 붙습니다.

> **📌 설계 판단 — 핸들러에는 위험 배지를 달지 않습니다.**
> `await` 뒤 setState 는 거의 모든 React 코드에 있는 형태이고, **React 18 은 이 경고를
> 없앴습니다.** 여기에 빨간 배지를 달면 "헛경보를 안 내는 쪽" 이라는 이 도구의 기준을
> 정면으로 어깁니다. **못 잡는 것이 아니라 판정을 넣지 않은 것**입니다.

---

## 4. 스코프 경계(Boundary) — 단일 파일 분석의 한계 표기

### 4-1. 문제 정의

Code Bunseki 는 **단일 파일(또는 붙여넣은 스니펫) 단위 도구**입니다. `import` 대상의 본문은 알 수 없습니다. 이는 고칠 수 있는 결함이 아니라 **입력 범위에서 오는 구조적 제약**입니다.

문제는 제약 자체가 아니라, 초기 구현에서 아래 세 가지가 모두 `상태 변화 없음` 이라는 **동일한 출력**으로 수렴했다는 점이었습니다.

1. 실제로 상태를 변경하지 않는 핸들러
2. prop 으로 전달받아 부모에 정의된 콜백
3. 다른 파일에서 import 한 훅에서 받아온 함수

사용자가 **"도구가 못 찾은 것"과 "실제로 없는 것"을 구분할 수 없었습니다.**

### 4-2. 해결 — `boundary` 스텝

흐름이 범위를 벗어나면 그 지점과 이유를 명시합니다.

```
·  여기서부터 범위 밖
   onChange   ← prop 으로 전달받은 함수
   이 컴포넌트를 사용하는 쪽에 있습니다.
   부모 코드도 함께 붙여넣으면 이어서 볼 수 있습니다
```

```
·  여기서부터 범위 밖
   setQuery   ← useSearch 에서 받아옴
   이 훅은 다른 파일에 있어 여기서부터는 따라갈 수 없습니다
```

**판정 로직** — 핸들러가 참조하는 식별자에 대해:

| 조건 | 결과 |
| :--- | :--- |
| 로컬 함수 목록에 있음 | 경계 아님 — 계속 추적 |
| import 한 훅의 구조분해 결과 | `boundary` — 훅 이름 명시 |
| 컴포넌트 파라미터(prop) 이름 | `boundary` — 부모 참조 안내 |
| 위 어디에도 없음 | 실제로 상태 미변경 |

### 4-3. 실무 활용

경계 표시는 **"어떤 코드를 더 붙여넣어야 하는가"** 에 대한 답입니다. 컴포넌트와 그 컴포넌트가 사용하는 커스텀 훅을 함께 붙여넣으면 연쇄가 끊기지 않습니다.

---

## 5. 정량적 메트릭 수치를 활용한 코드 리팩토링 전략

1. **God Component 해체 (라인 수 > 100, State Hook > 5개 → `🚨 Refactor` 배지)**
   - 로직 및 상태 관리를 **Custom Hook** 으로 모듈화
   - JSX 하위 뷰를 **Sub-component** 로 분리

2. **Effect 오남용 방지**
   - Hook Breakdown 에서 `useEffect` 비중이 높으면 `useMemo` 전환 또는 렌더 타임 계산으로 대체

3. **상태 연쇄(Cascade) 진단** ⭐ 동작 탭 활용
   - 동작 탭에서 `setter → effect → setter → effect` 가 반복되면 **파생 상태를 상태로 관리하고 있다는 신호**입니다.
   ```jsx
   // ❌ 불필요한 연쇄
   const [total, setTotal] = useState(0)
   useEffect(() => { setTotal(items.length) }, [items])

   // ✅ 렌더 타임 계산
   const total = items.length
   ```
   - 연쇄 깊이가 3단계를 넘으면 리렌더 횟수와 중간 상태 불일치를 의심하십시오.

4. **Props Drilling & 결합도 진단**
   - 동작 탭에서 `prop 으로 전달받은 함수` 경계가 여러 컴포넌트에 반복 등장하면 Context 또는 전역 상태 도입 검토

5. **헬퍼 함수 진단 및 분리**
   - **Dead Code 감지**: 플로우 차트 하단에 연결선 없이 독립된 헬퍼 카드 → 미사용 잔재 가능성
   - **비동기 예외 처리 점검**: `calls Async` 연결 함수는 `try/catch` 및 Error Boundary 확인 필수
   - **공통 유틸 분리**: 중복 호출되는 헬퍼는 `src/utils/` 로 추출하여 순수 함수로 캡슐화

---

## 6. 조각/부분 코드(Truncated Code) 분석 매커니즘

> ⚠️ **이 장은 2026-08-24 에 실측으로 다시 썼습니다.** 예전 문서는 "①~④ 는 조각 코드도
> 분석 가능" 이라고 적혀 있었지만, 그건 **정규식 파서 시절** 이야기입니다.
> `91f1a13` 에서 두 분석기가 모두 AST 기반이 되면서 **함께 실패합니다.**

**지금 동작 (실측)**

파싱이 실패하면 `parser.js` 는 `emptyResult(totalLines)` 를 돌려줍니다. 그 결과:

| 탭 | 잘린 코드에서 실제로 보이는 것 |
| :--- | :--- |
| ① 하이라이트 | 코드 자체는 그려지나 **섹션 밴드·배지 없음** |
| ② 구조맵 | **완전히 빈 화면** (안내 문구 없음) |
| ③ 메트릭 | 줄 수만 맞고 **나머지 전부 `0`** |
| ④ 플로우 | `컴포넌트 관계가 발견되지 않았습니다` |
| ⑤ 동작 | 오류 위치와 사유를 **정확히** 표시 |

```
문법 오류로 동작을 분석할 수 없습니다
예상하지 못한 토큰이 있습니다.
L11
동작 분석은 문법이 온전한 코드에만 적용됩니다. 다른 탭은 그대로 사용할 수 있습니다.
```

> 🚧 **알려진 문제**: 위 마지막 문장(*"다른 탭은 그대로 사용할 수 있습니다"*)은 **이제 사실이
> 아닙니다.** 또한 ③ 메트릭이 내놓는 `0` 들은 "세어 보니 없다" 가 아니라 "못 셌다" 인데
> 화면에서는 구분되지 않습니다. **조용히 틀린 결과를 내놓지 않는다**는 이 도구의 설계 원칙에
> 어긋나므로 고칠 대상입니다 (`HANDOFF.md` 6장 참고).

```
문법 오류로 동작을 분석할 수 없습니다
JSX 태그가 닫히지 않았습니다. 코드가 중간에 잘렸는지 확인해 주세요.
L30
```

`@babel/parser` 의 `errorRecovery: true` 는 복구 가능한 오류만 처리하며, 잘린 JSX(`UnterminatedJsxContent`) 같은 토크나이저 수준 실패는 예외를 발생시킵니다. 절단 후 재시도하는 휴리스틱 복구도 검토했으나, **부분 복구된 결과가 사용자에게 완전한 분석으로 오인될 위험**이 더 크다고 판단하여 채택하지 않았습니다.

> **설계 원칙**: 조용히 틀린 결과를 내놓느니 실패 지점을 정확히 알리는 편이 낫습니다.
> 예전 정규식 파서는 잘린 함수를 `Math.min(startIdx + 1, …)` 로 처리해 **2줄짜리로 보고**했고,
> 사용자는 그것이 틀렸다는 사실을 알 방법이 없었습니다. AST 로 옮기며 그 문제는 사라졌지만,
> **위에 적은 "조용한 0"** 이라는 같은 종류의 문제가 ①~④ 탭에 남아 있습니다.

---

## 7. 도구가 보장하는 것과 보장하지 않는 것

### ✅ 보장하는 것

- **구문이 완전한 코드에서 오탐(false positive)을 내지 않는 쪽으로 설계했습니다.**
  애매하면 경고하지 않고 넘어갑니다 — 즉 **경고가 없다는 것이 문제가 없다는 뜻은 아닙니다.**
  반대로 **뜬 경고는 대체로 진짜**입니다. 규칙을 넓힐 때마다 "잡으면 안 되는 것" 테스트를
  함께 늘려 이 성질을 지킵니다.
- 모든 스텝은 소스 라인 근거를 가지며 클릭으로 검증 가능합니다.
- 동작 분석 실패가 다른 탭에 전파되지 않습니다.

### ⚠️ 보장하지 않는 것

**1. 완전성 (Completeness)**  
붙여넣은 범위 밖의 흐름은 추적하지 않습니다. 다만 그 지점을 `boundary` 로 명시하므로, 누락이 침묵하지는 않습니다.

**2. 실행 경로 (Runtime Path)**  
정적 분석은 **가능한 경로**를 보여줄 뿐 **실제 실행**을 알지 못합니다.

```jsx
useEffect(() => {
  if (!open) return        // ← 런타임 값에 따라 조기 반환
  fetchData()
}, [open])
```

`open` 이 `false` 로 전이될 때도 `fetchData()` 스텝은 표시됩니다.
**다만 "조건 분기가 표현되지 않는다" 는 더 이상 사실이 아닙니다** — 되돌아 나갈 수 있는
자리는 `↩ !open 면 중단` **관문 스텝**으로 그려집니다([3-10](#3-10-조건부-실행--관문gate)).
표현되지 않는 것은 **그 조건이 실제로 참인지 거짓인지**입니다.

**3. 미지원 패턴**  
클래스 컴포넌트, Context 값 전파, 파일 간 컴포넌트 트리 추적은 현재 범위 밖입니다.

### 📌 함의

**"잘 짜인 코드일수록 분석이 잘 된다"는 성립하지 않습니다.** 커스텀 훅 추출, 프레젠테이션 컴포넌트 분리, prop 콜백은 모두 권장 패턴이지만 단일 파일 분석에는 불리하게 작용합니다. 로직이 한 컴포넌트에 집중된 코드가 오히려 완전하게 분석됩니다.

이 역설을 완화하기 위해 **같은 파일 안의 커스텀 훅은 끝까지 추적**하고, 파일을 벗어나는 지점은 **경계로 명시**하는 방향을 택했습니다.

---

## 8. 협업 및 문서화 (Export & Markdown Report)

- 상단 **[Export]** 버튼 클릭 시 코드 구조 및 정량 메트릭이 Markdown 으로 클립보드에 복사됩니다.
- Pull Request 본문, 코드 리뷰 문서, 아키텍처 보고서에 즉시 활용 가능합니다.

> **현재 Export 범위**: **구조 분석기**(`parser.js`)가 뽑은 메트릭·컴포넌트·플로우 정보만
> 포함되며, **동작 분석 연쇄(대기·관문·위험·Effect 관계)는 포함되지 않습니다.**
> 파일로 저장되는 것이 아니라 **클립보드에 복사**됩니다.

---

## 9. 검증 체계

판정 규칙이 많아질수록 **넓힌 규칙이 조용히 헛경보를 만드는지**가 문제가 됩니다.
그래서 세 층으로 확인합니다.

| 층 | 무엇을 보나 | 실행 |
| :--- | :--- | :--- |
| 단위 테스트 (142개) | 엔진이 내놓는 **데이터** | `npm test` (Node 내장 러너, 의존성 0) |
| 브라우저 렌더 확인 | 렌더러가 **그리는 결과** — 라벨 HTML 주입 0, 가로 넘침 0 | `test/browser-verify.html` |
| 실제 앱 확인 | 조립된 **앱이 실제로 도는가** | [`VERIFICATION.md`](./VERIFICATION.md) |

**테스트 구성**: `parser` 10 · `behavior` 29 · `examples` 3 · `timing` 51 ·
`stale-deps` 12 · `interplay` 19 · `hook-boundary` 18.

**핵심 규약** — 판정 규칙을 넓힐 때는 각 테스트 파일의 **"잡으면 안 되는 것"** 절을
먼저 확인합니다. 이 도구의 가치는 잡는 개수가 아니라 **틀리게 말하지 않는 것**에 있습니다.

> 실제로 이번 라운드에서 나온 수정 넷은 모두 "못 잡는 것" 이 아니라 **"틀리게 적는 것"** 이었습니다
> — 응답 전 이른 반환을 가드로 셈 · `try` 안 가드를 못 봄 · `dispatch` 를 언마운트 위험으로 셈 ·
> 상태가 안 바뀌는데 "리렌더" 를 적음.

### 보안 관련 규약

`ui/behavior.js` 에서 `innerHTML` 에 **코드에서 온 문자열**을 넣을 때는 반드시 `esc()` 를
거칩니다. 안 그러면 `<button onClick>` 같은 라벨이 **진짜 엘리먼트로 렌더**됩니다
(실제로 발생했던 버그). 브라우저 확인에 **라벨 HTML 주입 0건** 항목이 있는 이유입니다.
