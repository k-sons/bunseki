# 지금 bunseki 는 이렇게 되어 있습니다

> 갱신: 2026-09-01  
> **이 앱이 지금 무엇을 보여 주는지** 남에게 설명할 때 쓰는 스냅샷입니다.  
> 개발 이력·다음 할 일은 [HANDOFF.md](./HANDOFF.md), 사용법은 [GUIDE.md](./GUIDE.md) 입니다.

---

## 한 줄

React / React Native 코드 **한 파일**을 붙여넣으면, **무엇이 있는지**(네 탭)와 **버튼을 누르면 무슨 일이 일어나는지**(동작 탭)를 보여 주는 브라우저 도구입니다.

실행하지 않습니다. 코드를 읽어서 그립니다. Vite + Vanilla JS, 프레임워크 없음.

```bash
npm run dev    # http://localhost:5173
npm test       # 엔진 테스트 (이 화면 문구는 커버하지 않음)
```

---

## 다섯 탭 — 각각 묻는 질문이 다릅니다

| 탭 | 묻는 질문 | 엔진 |
| :--- | :--- | :--- |
| 🎨 하이라이트 | 이 줄은 무슨 역할인가 | `parser.js` |
| 🗺️ 구조맵 | 이 파일 안에 뭐가 있나 | `parser.js` |
| 📊 메트릭 | 어디가 비대하고 Hook 이 몰렸나 | `parser.js` |
| 🔄 플로우 | 컴포넌트·함수가 어떻게 연결되나 | `parser.js` |
| ⚡ 동작 | **버튼을 누르면**, 그리고 **이 Effect 가 위험한가** | `behavior/` |

앞의 넷은 **"무엇이 있는가"**. 동작 탭만 **"어떻게 도는가"**.

문법이 깨져 있으면 숫자를 `0` 으로 보여 주지 않습니다. **"못 읽었다"** 고 말합니다. 하이라이트만 안내 + 코드를 같이 보여 잘린 자리를 볼 수 있게 합니다.

---

## ⚡ 동작 탭은 세 덩어리입니다

위에서부터 역할이 갈립니다. **합치지 않습니다.**

```
① 이벤트 연쇄          ← "버튼을 누르면"
   화면 · React  |  동작 흐름

② 이 Effect는 언제·무엇을 보나?     ← "이 Effect가 위험한가"
③ Effect끼리 서로 건드리나?        ← 같은 질문, Effect 사이
```

① 은 두 칸입니다. 화면이 바뀌는 일과 뒤에서 흐르는 제어를 같은 시간축에 나란히 둡니다.
②·③ 은 가로 알약입니다. 그림이 다른 이유는 **묻는 질문이 다르기 때문**입니다.

상태나 Effect 가 커스텀 훅 안에 있으면 세 곳 모두 **🪝 훅**이 붙습니다.
① 은 스텝을 상자로 묶고, ②·③ 은 배지입니다.

---

## ① 버튼을 누르면 — 두 칸 연쇄

상단에 이벤트 칩(`<button onClick> L13`)이 있고, 고르면 아래가 채워집니다.

| 칸 | 무엇이 오나 |
| :--- | :--- |
| 화면 · React | 클릭, 리렌더처럼 **눈에 보이는 변화** |
| 동작 흐름 | 함수 호출, Effect, 기다림, 관문처럼 **뒤의 제어** |
| 맞물림 ↔ | `setOpen()` 처럼 **상태가 바뀌어 두 칸이 만나는 자리** |

흐름만 움직일 때 화면 칸에는 **"화면 유지"** 가 남습니다. 그동안 화면은 이전 그대로라는 뜻입니다.

그 밖에 읽는 표시:

- **⏳ 기다리는 구간** — 세로 높이로 시간이 흐르는 자리
- **↩ 관문** — 조건이 참이면 되돌아 나가는 자리. 번호는 먹지 않습니다
- **🟠 오류 시** — `.catch` · `try/catch` 에서만 가는 길
- **주황 점선** — 이 파일 밖이라 더 못 봄 (`prop` 으로 받은 함수, 다른 파일의 훅)

---

## ②③ 이 Effect 가 위험한가 — 말하는 방식만 바꾼 자리

분석은 그대로입니다. **제목·한 줄 결론·접기·단어**만 바꿨습니다.

두 칸 연쇄와 그림이 달라서 어렵게 느껴졌고, 그래서 연쇄에 맞추지 않고 **질문으로 말하게** 바꿨습니다.
두 축으로 합치거나 숨기지 않습니다. 여기는 "버튼을 누르면"이 아닙니다.

### 제목이 질문입니다

| 화면 제목 | 예전 말 | 배지 |
| :--- | :--- | :--- |
| **이 Effect는 언제·무엇을 보나?** | ⏱ 타이밍 · deps 점검 | `위험 N` · `목록에 없는 값 N` |
| **Effect끼리 서로 건드리나?** | 🔗 Effect 사이 관계 | `무한 루프 N` · `누가 마지막에 쓰나 N` |

단어 교체:

- `deps 빠짐` → **목록에 없는 값**
- `경합` → **누가 마지막에 쓰나**
- `deps [isPolling] 변경 시` → **`의존 목록 [isPolling] 변경 시`**
- 경고 박스 설명에만 **`(deps)`** 를 한 번 병기합니다. 칩·머리말에는 안 씁니다.

엔진 안에서는 여전히 `staleDeps` · `contention` 입니다. 화면 말만 바꿨습니다.

### 카드는 결론이 먼저, 알약은 근거

```
한 줄 결론
  "답이 오기 전에 화면이 사라지면, 없는 화면에 상태를 바꿉니다"
  "[userId]가 의존 목록에 없어 옛 값을 볼 수 있습니다"
  "서로 바꿔서 화면이 멈추지 않을 수 있습니다"

그 아래 가로 알약 = 근거 (언제 실행 → 대기 → setState …)
```

관계 카드의 종류 배지:

| 배지 | 뜻 |
| :--- | :--- |
| 무한 루프 | 스스로 또는 서로 되받아 안 멈출 수 있음 |
| 누가 마지막에 쓰나 | 같은 상태를 Effect 여럿이 바꿈 |
| 이어서 실행 | 하나가 바꾸면 다음 Effect 가 따라 실행 (정보) |

### 처음엔 접혀 있습니다

| 코너 | 처음부터 열리는 때 |
| :--- | :--- |
| 언제·무엇을 보나? | **위험**이 있거나 **목록에 없는 값**이 있을 때 |
| 서로 건드리나? | **무한 루프** 또는 **위험**(늦게 온 답이 덮을 수 있음)이 있을 때 |

타이밍 코너는 비동기인데 가드가 있는 Effect만 있으면 접힙니다.
관계 코너는 동기 경합·연쇄만 있을 때 접힙니다. **루프는 가드가 있어도 펼칩니다.**
아무것도 짚을 것이 없으면 코너 자체가 안 나옵니다.

---

## 일부러 안 하는 것

설명하다 "왜 없지?" 가 나오면 이 목록입니다.

- **실행하지 않음.** 실제 값·실제 ms 는 모릅니다. 대기 폭은 상대 눈금입니다.
- **파일 하나.** `import` 한 다른 파일은 못 봅니다. 그때는 주황 점선으로 "여기서부터 못 봄"을 표시합니다.
- **②③ 을 두 축으로 합치지 않음.** 질문은 "버튼을 누르면"이 아니라 "이 Effect 가 위험한가"입니다.
- **시간별 값 스냅샷(CCTV) 없음.** 두 축으로도 안 풀릴 때만 검토. 지금은 안 함.
- **핸들러의 await 뒤 setState 에 빨간 위험 배지 없음.** React 18 이 이 경고를 없앤 형태라, 달면 헛경보가 됩니다. 기다리는 구간·관문은 연쇄에만 그립니다.
- **헛경보를 안 내는 쪽이 검사의 전부입니다.** 확실한 것만 말합니다.

---

## 지금 화면과 설명이 살짝 다른 곳

남에게 말할 때 과장하지 않으려고 적어 둡니다.

1. **타이밍 카드**는 맨 위가 결론이 아닙니다. 순서: 머리말(언제·칩·줄번호) → **한 줄 결론** → 알약 → 경고 박스. 결론이 알약 *위*인 것은 맞습니다.
2. **관계 카드**의 한 줄은 종류별 고정 문장입니다. 엔진이 만든 `L12 이 스스로를 다시 부릅니다` 같은 구체 제목은 화면에 안 나옵니다. 줄 번호·상태 이름은 알약과 아래 설명에 있습니다.
3. [HANDOFF.md](./HANDOFF.md) 본문의 `⏱ 타이밍 · deps 점검` / `경합` 은 **예전 화면 말**입니다. 지금 화면은 이 문서를 따르세요.

---

## 코드가 어디에 있나

설명할 때 "그건 어느 파일?" 에 답하는 지도입니다.

```
붙여넣은 코드
        │
  ┌─────┴──────┐
  ▼            ▼
parser.js    behavior/
무엇이 있나    어떻게 도나
  │            │
①②③④        ⑤ 동작
```

| 파일 | 역할 |
| :--- | :--- |
| `src/core/parser.js` | 구조 분석 — 네 탭 |
| `src/core/behavior/` | 동작 분석 — 연쇄·타이밍·deps·관계 |
| `src/core/behavior/rails.js` | 연쇄 스텝을 화면/흐름 두 칸으로 **갈라 놓기만** (새 분석 없음) |
| `src/ui/behavior.js` | 동작 탭 화면. ②③ 의 질문·결론·접기가 여기 |
| `src/styles/index.css` | `.inspect-verdict` · `<details>` 접기 화살표 |
| `test/browser-verify.html` | 브라우저에서 제목·펼침·결론 문구를 로그로 확인 |

화면 말을 고칠 파일은 **`src/ui/behavior.js`** 입니다. 판정을 고칠 일은 보통 `timing.js` · `deps.js` · `interplay.js` 입니다.

---

## 이렇게 말하면 됩니다

> bunseki 는 코드를 붙여넣으면 다섯 탭으로 보여 줍니다.  
> 앞의 넷은 파일 안에 **무엇이 있는지**이고, 동작 탭은 **어떻게 도는지**입니다.
>
> 동작 탭 위쪽은 **"이 버튼을 누르면"**. 화면이 바뀌는 일과 뒤의 제어를 두 칸으로 나란히 둡니다.
>
> 아래 둘은 **"이 Effect 가 위험한가"** 입니다. 그림이 다른 이유가 그겁니다.  
> 제목이 질문이고, 카드마다 한 줄 결론이 먼저 오고, 가로 알약은 근거입니다.  
> 위험이나 루프가 있을 때만 처음부터 열려 있습니다.
>
> `deps 빠짐` 같은 말은 안 씁니다. **목록에 없는 값**, **누가 마지막에 쓰나** 라고 말합니다.
>
> 분석 엔진은 그대로입니다. 말하는 방식만 바꿨습니다.
