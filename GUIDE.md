# 📘 Code Bunseki — 가이드 통합 센터 (Guide Index)

> **Code Bunseki** 가이드 센터에 오신 것을 환영합니다!  
> 사용자의 숙련도와 목적에 따라 아래 두 가지 맞춤형 가이드 문서 중 하나를 선택하여 확인하실 수 있습니다.

---

## 🔬 Code Bunseki 는 무엇을 하나요?

React / React Native 코드를 붙여넣으면 **5개의 탭**으로 나누어 분석해 줍니다.

| 탭 | 답해 주는 질문 |
| :--- | :--- |
| 🎨 **하이라이트** | 이 코드의 어느 구역이 무슨 역할인가? |
| 🗺️ **구조맵** | 이 파일 안에 무엇이 들어 있는가? |
| 📊 **메트릭** | 어디가 비대하고 어디에 Hook 이 몰려 있는가? |
| 🔄 **플로우** | 컴포넌트와 함수가 서로 어떻게 연결돼 있는가? |
| ⚡ **동작** | **버튼을 누르면 실제로 무슨 일이 일어나는가?** |

앞의 네 탭이 **"무엇이 있는가"** 를 보여 준다면, **동작** 탭은 **"어떻게 동작하는가"** 를 보여 줍니다.

```
버튼 클릭 → setOpen() → open 변경 → deps [open] 이 바뀌어 useEffect 재실행
         → fetchData() 비동기 → setData() → data 변경 → 리렌더
```

---

## 📚 맞춤 가이드 문서 안내

### 1. 🐣 [왕초보용 100% 입문 가이드 (GUIDE_BEGINNER.md)](./GUIDE_BEGINNER.md)
**대상**: React / React Native 입문자, 코딩 초보자, 쉬운 설명이 필요한 분
- **주요 내용**:
  - **🚀 5분 안에 시작하기 — 켜는 법 · 넣는 법 · `Ctrl+Enter` · 버튼별 하는 일**
  - 알록달록 색상 배지로 1초 만에 코드 구획 읽기 (레고 비유)
  - 좋은 코드 vs 치료가 필요한 위험한 코드 셀프 진단법
  - 구조맵 / 메트릭 / 플로우 탭 클릭을 통한 1초 코드 교차 검증법
  - **동작 탭으로 "이 버튼을 누르면 무슨 일이 생기나" 따라 읽기**
  - **주황 점선 = "여기서부터는 못 봐요" 신호 읽는 법**
  - **⏳ 기다리는 구간 · ↩ 관문 · 🟠 오류 시 · 🪝 훅 구역 읽는 법**
  - **⏱ 타이밍 · deps 점검 / 🔗 Effect 사이 관계 — 아래쪽 두 점검 코너**
  - **🚨 꼭 알아 둘 주의사항 8가지**
  - 안 쓰는 헬퍼 함수 지우기 등 3분 코드 청소법
  - 잘린 조각 코드는 어디까지 되고 어디부터 안 되는지

---

### 2. 📘 [개발자/실무자용 메트릭 & 아키텍처 가이드 (GUIDE_PRO.md)](./GUIDE_PRO.md)
**대상**: 현업 프론트엔드 개발자, 코드 리뷰어, 아키텍처 개선 검토자
- **주요 내용**:
  - 두 개의 분석 엔진 — 구조 파서(`src/core/parser.js`)와 동작 파서(`src/core/behavior/`)
    ※ 두 파서 모두 **AST 기반**입니다. `GUIDE_PRO.md` 본문에는 아직 `parser.js` 를
    "정규식 파서" 로 적은 곳이 남아 있습니다 — 그 부분은 현재 구현과 다릅니다.
  - 5개 분석 대시보드(Highlight, Structure Map, Metrics, DAG Flow, **Behavior**) 기술 명세
  - 라인 범위(`L11–L33`) 및 호버 툴팁을 활용한 Traceability 이중 검증
  - **상태 변화 연쇄 추적** — 이벤트 → setter → 상태 → deps 일치 Effect → 비동기 → 리렌더
  - **커스텀 훅 추적 및 스코프 경계(Boundary) 표기**
  - God Component 감지 및 헬퍼 함수 분리 전략
  - Export 마크다운 보고서를 활용한 PR 및 협업 워크플로우
  - 정적 분석의 한계와 도구가 보장하는 것 / 보장하지 않는 것

---

## ⚠️ 먼저 알아 두면 좋은 것

Code Bunseki 는 **파일 하나를 붙여넣어 쓰는 도구**입니다. `import` 로 가져온 다른 파일의 내용은 볼 수 없습니다.

그래서 흐름이 붙여넣은 코드 밖으로 이어질 때는 **조용히 넘어가지 않고 그 지점을 표시**합니다.

- `prop 으로 전달받은 함수` — 이 컴포넌트를 쓰는 쪽(부모)에 있습니다
- `useSearch 에서 받아옴` — 그 훅이 다른 파일에 있습니다

**"도구가 못 찾은 것"과 "실제로 아무 일도 안 일어나는 것"을 구분할 수 있도록** 만든 표시입니다. 자세한 내용은 각 가이드를 참고하세요.

---

## 📎 그 밖의 문서

- [✅ VERIFICATION.md](./VERIFICATION.md) — **실제 앱을 브라우저에서 눌러 확인한 기록**
  (빌드 · 5개 탭 · 붙여넣기 · TypeScript · 2577줄 대용량 · 오탐 점검 · 성능)
- [🔄 HANDOFF.md](./HANDOFF.md) — 개발 인계문 (현재 상태와 다음 할 일)
