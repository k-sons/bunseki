# 🐣 Code Bunseki — 왕초보를 위한 100% 쉬운 코드 건강 검진 가이드 (Beginner Version)

> **"코딩을 막 시작했는데, 내가 짠 코드가 좋은 코드인지 아닌지 어떻게 알죠?"**  
> **"버튼을 누르면 도대체 무슨 일이 벌어지는 거지?"**  
> **Code Bunseki**는 코드를 입력하면 마치 의사가 청진기로 검진하듯, 코드의 모양과 **동작 순서**를 알록달록한 색상과 그래프로 보여 주는 **코드 검진 도구**입니다!

---

## 📑 목차
0. [🚀 5분 안에 시작하기 (사용법)](#0--5분-안에-시작하기-사용법)
1. [🩺 이 도구는 무엇인가요? (코드 건강 검진기)](#1--이-도구는-무엇인가요-코드-건강-검진기)
2. [🌈 1초 만에 이해하는 알록달록 색상 공식](#2--1초-만에-이해하는-알록달록-색상-공식)
3. [🏥 내 코드 건강 상태 진단법 (좋은 코드 vs 위험한 코드)](#3--내-코드-건강-상태-진단법-좋은-코드-vs-위험한-코드)
4. [🖥️ 화면의 5가지 탭 아주 쉽게 읽는 법](#4-️-화면의-5가지-탭-아주-쉽게-읽는-법)
5. [⚡ 동작 탭 완전 정복 — 버튼을 누르면 무슨 일이?](#5--동작-탭-완전-정복--버튼을-누르면-무슨-일이)
6. [🧹 왕초보도 따라 하는 3분 코드 청소(리팩토링) 법](#6--왕초보도-따라-하는-3분-코드-청소리팩토링-법)
7. [❓ FAQ: 잘린 코드도 검진되나요?](#7--faq-잘린-코드도-검진되나요)
8. [🚨 꼭 알아 둘 주의사항 8가지](#8--꼭-알아-둘-주의사항-8가지)

---

## 0. 🚀 5분 안에 시작하기 (사용법)

### ① 앱 켜기

```bash
npm install       # 처음 한 번만
npm run dev       # → http://localhost:5173 이 뜹니다
```

터미널에 나온 주소를 브라우저에서 열면 끝입니다. **설치도 로그인도 없습니다.**

### ② 코드 넣기 — 세 가지 방법

| 방법 | 어떻게 |
| :--- | :--- |
| **붙여넣기** (가장 많이 씁니다) | 왼쪽 큰 칸에 `Ctrl+V` |
| **예제 보기** | 위쪽 **예제** 버튼 — 누를 때마다 **다른 예제로 바뀝니다** |
| **직접 타이핑** | 왼쪽 칸에 그냥 쓰면 됩니다 (`Tab` 은 2칸 들여쓰기) |

### ③ 언어 고르기

왼쪽 위 선택 상자에서 고릅니다. **색칠이 달라집니다.**

- `JSX / React` ← 기본값. 보통 이거면 됩니다
- `TSX / TypeScript` ← `.tsx` 파일이라면 이걸로
- `JavaScript` / `TypeScript` ← React 가 아닌 코드

### ④ 분석하기

**`분석` 버튼**을 누르거나, 코드 칸에서 **`Ctrl+Enter`** (맥은 `Cmd+Enter`).

### ⑤ 결과 읽기

오른쪽 위 **탭 5개**를 눌러 가며 봅니다. 어떤 탭이 무엇을 답해 주는지는
[4장](#4-️-화면의-5가지-탭-아주-쉽게-읽는-법)에 있습니다.
**대부분의 항목은 클릭하면 왼쪽 코드의 해당 줄로 이동합니다.**

### ⑥ 그 밖의 버튼

| 버튼 | 하는 일 |
| :--- | :--- |
| **내보내기** | 분석 결과를 **마크다운으로 클립보드에 복사**합니다 (파일로 저장되는 게 아닙니다 — 붙여넣어 쓰세요) |
| **지우기** | 넣은 코드를 비웁니다 |
| **테마** | 밝게/어둡게 전환 (이 설정만 기억됩니다) |
| 가운데 경계선 | **드래그하면** 코드 칸과 결과 칸의 너비를 바꿀 수 있습니다 |

> ⚠️ **붙여넣은 코드는 저장되지 않습니다.** 새로고침하면 사라집니다.
> 기억되는 건 **밝게/어둡게 설정 하나뿐**입니다.

---

## 1. 🩺 이 도구는 무엇인가요? (코드 건강 검진기)

우리가 레고를 만들 때 상자 안에 어떤 블록이 몇 개 들어있는지 확인하듯, React 코드를 작성할 때도 **데이터 블록, 화면 그리기 블록, 버튼 클릭 블록**이 골고루 잘 섞여 있는지 확인해야 합니다.

**Code Bunseki**에 코드를 쏙 넣으면, 복잡한 문자 대신 **색상 배지**, **상자 지도**, **키 재기 그래프**, 그리고 **동작 순서표**로 변환하여 초보자도 한눈에 이해할 수 있게 도와줍니다.

특히 마지막 **동작(⚡) 탭**이 핵심입니다. 초보자가 React 에서 가장 헷갈리는 것이 바로 이것이거든요:

> "버튼을 눌렀는데... 왜 화면이 두 번 바뀌지?"  
> "`useEffect` 는 도대체 언제 다시 실행되는 거야?"

동작 탭은 이 질문에 **순서대로 번호를 매겨** 답해 줍니다.

---

## 2. 🌈 1초 만에 이해하는 알록달록 색상 공식

### 📍 코드 구획을 나누는 큰 색깔 띠 (섹션 밴드)

하이라이트 화면에서 코드 줄 배경에 깔리는 색과 왼쪽 라벨입니다.

| 라벨 | 색 | 하는 일 | 레고 비유 |
| :--- | :--- | :--- | :--- |
| `IMPORT` | 🔵 파랑 | 외부 도구나 부품 가져오기 | 레고 상자 열기 |
| `CONST` | 🟢 초록 | 안 바뀌는 고정 데이터 | 설명서에 적힌 숫자 |
| `COMPONENT` | 🟣 보라 | 화면을 그리는 조각 | 완성된 레고 작품 |
| `HELPER` | 🟡 노랑 | 도와주는 일반 함수 | 도구 상자 |
| `EXPORT` | 🟠 주황 | 다른 파일에 내보내기 | 완성품 포장 |

### 🏷️ 줄 끝에 붙는 작은 배지

| 배지 색 | 뜻 | 예시 |
| :--- | :--- | :--- |
| 🟠 주황 | **Memo** — 계산 결과를 아껴 두기 | `useMemo`, `useCallback` |
| 🟢 초록 | **State** — 화면이 기억하는 데이터 | `useState`, `useReducer` |
| 🩵 청록 | **RN** — React Native 전용 | `StyleSheet`, `Animated` |
| 🔵 파랑 | **Effect** — 뒤에서 몰래 일하는 비서 | `useEffect`, `useLayoutEffect` |
| 🟦 남보라 | **Async** — 시간이 걸리는 일 | `async`, `await` |
| 🟣 보라 | **Store** — 앱 전체가 함께 쓰는 데이터 | `useSelector`, `dispatch` |
| 🩷 분홍 | **Handler** — 버튼 클릭·글자 입력 반응 | `onClick`, `onChange` |

> 💡 배지 색만 봐도 "여긴 데이터 기억 구역이구나", "여긴 버튼 반응 구역이구나" 감이 옵니다.
>
> 위 표는 **무지개 순서**로 정렬돼 있습니다. 색이 서로 충분히 떨어져 있어서, 한 줄에 배지가 여러 개 붙어도 구별됩니다. 예를 들어 `const handleClick = useCallback(...)` 에는 🩷 **분홍(Handler)** 과 🟠 **주황(Memo)** 이 나란히 붙습니다.

---

## 3. 🏥 내 코드 건강 상태 진단법 (좋은 코드 vs 위험한 코드)

코드를 입력하고 스크롤을 내릴 때 아래 두 가지만 확인하세요!

### ✅ 🟢 건강한 코드의 모습
- 스크롤을 조금만 내려도 화면 그리기(JSX) 부분이 금방 나타납니다.
- 🟢 **State** 나 🔵 **Effect** 배지가 적당히 예쁘게 섞여 있습니다.
- 동작 탭의 연쇄가 **3~5단계 안에서 끝납니다.**

### ⚠️ 🚨 치료(리팩토링)가 필요한 위험한 코드
- 화면 그리기 부분을 보려면 스크롤을 한참 내려야 합니다.
- 위에 🟢 **State** 와 🔵 **Effect** 배지가 빽빽하게 도배되어 있습니다.
- 동작 탭에서 **연쇄가 끝없이 이어집니다** — 상태 하나 바뀌었는데 Effect 가 또 상태를 바꾸고, 그게 또 다른 Effect 를 깨우는 식입니다.
- 👉 **진단**: 한 컴포넌트가 너무 많은 일을 혼자 다 하려고 해서 과로 중입니다! (다이어트 필요)

> 🚨 **Refactor 배지**를 조심하세요  
> 구조맵에서 컴포넌트 이름 옆에 빨간 `🚨 Refactor` 배지가 붙었다면, 그 컴포넌트가 **100줄을 넘었거나 Hook 을 5개 넘게** 쓰고 있다는 뜻입니다.

---

## 4. 🖥️ 화면의 5가지 탭 아주 쉽게 읽는 법

### 🎨 1) 하이라이트 (Highlight) 탭
> **"코드에 무지개 옷을 입혀서 역할별로 구별해 줍니다!"**
- 알록달록한 배지를 보며 "아, 여긴 버튼 클릭 처리 구역이구나!" 감을 잡을 수 있습니다.

### 🗺️ 2) 구조맵 (Structure Map) 탭
> **"이 파일 안에 들어있는 상자(컴포넌트) 목록 정리함입니다!"**
- 컴포넌트 상자 이름 옆에 붙은 **`L11–L33`** 라인 번호를 보세요.
- **💡 아무 항목이나 클릭해 보세요!** 즉시 해당 코드 라인으로 자동 이동합니다.

### 📈 3) 메트릭 (Metrics) 탭
> **"누가 제일 키가 큰가? 막대 그래프 키 재기!"**
- **혼자 독보적으로 길게 솟아있는 막대**를 찾으세요! 그게 다이어트 대상입니다.
- **💡 막대에 마우스를 올리면** `useState (L25, L175)` 처럼 감지한 라인이 툴팁으로 뜨고, **클릭하면** 그 코드로 바로 이동합니다.

### 🔄 4) 플로우 (Flow) 탭
> **"컴포넌트와 함수들이 서로 어떻게 연결돼 있는지 보여주는 지도!"**

#### 📦 상자 종류
- 🟣 **Component (보라색)**: 화면을 그리는 React 컴포넌트 상자
- 🟡 **Function (노란색)**: 도움을 주는 일반 헬퍼 함수 상자

#### ⬇️ 화살표와 단어의 뜻
- ─── `renders` (실선): **"화면에 직접 그려준다!"**
- - - `calls` (점선): **"일 시키려고 불러서 실행한다!"**
- 🩵 `calls Async` (하늘색 + **Async** 태그): **"시간이 걸리는 비동기 일을 시킨다!"**

#### 💡 읽기 요령
1. 맨 위 시작 상자부터 화살표를 따라 내려가면 일 처리 순서가 보입니다.
2. 맨 아래에 **화살표가 하나도 안 붙은 노란 상자**가 있다면 → 아무도 안 쓰는 코드일 수 있습니다!

### ⚡ 5) 동작 (Behavior) 탭 — **가장 중요합니다**
> **"버튼을 누르면 실제로 무슨 일이 순서대로 일어나는지 보여줍니다!"**

다음 장에서 자세히 다룹니다.

---

## 5. ⚡ 동작 탭 완전 정복 — 버튼을 누르면 무슨 일이?

### 🎯 쓰는 법은 두 단계뿐입니다

**① 위쪽에서 궁금한 이벤트를 고릅니다**

```
Card
 [button onClick L13]

UserPanel
 [input onChange L61]  [button onClick L62]  [button onClick L63]
```

칩(작은 알약 모양 버튼)을 클릭하면 보라색으로 바뀌면서 선택됩니다.

**② 아래에 순서표가 나타납니다**

```
 ① 이벤트                    L13
    <button onClick>
         ↓
 ② 상태 변경                 L13
    setOpen()  →  open
         ↓  deps [open] 에 'open' 가 있어 다시 실행됩니다
 ③ Effect 실행                L8
    useEffect 재실행   [.then]
         ↓
 ④ 함수 호출                 L10
    fetchData()   [비동기]
         ↓
 ⑤ 상태 변경
    setData()  →  data
         ↓
 ⑥ 화면 갱신
    리렌더
```

**한국어로 풀면**: "열기 버튼을 누르면 → `open` 이 바뀌고 → `open` 을 지켜보던 `useEffect` 가 깨어나서 → 서버에서 데이터를 받아오고 → `data` 에 저장하고 → 화면을 다시 그린다."

> 💡 **각 줄을 클릭하면 그 코드로 바로 이동합니다!** 번호만 따라가도 코드 읽는 순서가 잡힙니다.

### 🎨 왼쪽 색 막대가 알려주는 것

| 색 | 단계 종류 |
| :--- | :--- |
| 🔵 파랑 | 이벤트 (여기서 시작) |
| 🟣 보라 | 상태 변경 |
| 🟡 노랑 | Effect 실행 |
| 🟢 초록 | 함수 호출 |
| ⚪ 회색 | 화면 갱신 (여기서 끝) |
| 🟠 **주황 점선** | **여기서부터는 못 봐요** ← 아래 설명! |

### ⏳ 넓은(높은) 칸 = "여기서 시간이 흘러요"

서버에서 답이 오기를 기다리는 자리는 **다른 칸보다 크게** 그려집니다.

```
 ③ 함수 호출    fetchData()   [비동기]
         ↓
 ⏳ 응답 대기   ≈3초            ← 칸이 눈에 띄게 큽니다
         ↓
 ④ 상태 변경    setData()  →  data
```

- `≈3초` — 코드에 시간이 **적혀 있어서 알아낸 것**입니다 (`sleep(3000)` 같은 경우)
- `시간 미상` — 서버가 언제 답할지는 코드만 봐서는 알 수 없습니다. **그래도 기다린다는 사실은 확실**합니다

**이 칸을 기준으로 위는 "누르자마자", 아래는 "답이 온 뒤"** 입니다.
`setLoading(true)` 가 위에, `setLoading(false)` 가 아래에 있으면 로딩 표시가 제대로 도는 겁니다.

> 💡 **버튼 핸들러 안에서 기다리는 것도 똑같이 보여 줍니다.**
> `onClick={async () => { await 저장(); setDone(true) }}` 같은 코드도
> `<button onClick> → ⏳ 응답 대기 → setDone()` 로 나옵니다.

### 🟠 "오류 시" 딱지 = "잘못됐을 때만 가는 길"

```
 ⏳ 응답 대기
         ↓
 ④ 상태 변경   setData()  →  data
 ⑤ 상태 변경   setError()  →  error   [오류 시]   ← 실패했을 때만!
```

`.catch(...)` 나 `try/catch` 안에서만 바뀌는 상태에 붙습니다.
**위험하다는 뜻이 아니라 "항상 불리지는 않는다"** 는 뜻입니다.

> `.finally` 안의 코드는 성공하든 실패하든 **둘 다** 실행되므로 이 딱지가 붙지 않습니다.

### 🚧 주황 점선 = "여기서부터는 붙여넣은 코드 밖이에요"

Code Bunseki 는 **여러분이 붙여넣은 코드만** 볼 수 있습니다. 다른 파일은 못 봅니다.

그래서 흐름이 바깥으로 이어지면 **조용히 넘어가지 않고 이렇게 알려 줍니다**:

```
 ① 이벤트                    L18
    <input onChange>
         ↓
 ·  여기서부터 범위 밖
    onChange   ← prop 으로 전달받은 함수
    이 컴포넌트를 사용하는 쪽에 있습니다.
    부모 코드도 함께 붙여넣으면 이어서 볼 수 있습니다
```

```
 ·  여기서부터 범위 밖
    setQuery   ← useSearch 에서 받아옴
    이 훅은 다른 파일에 있어 여기서부터는 따라갈 수 없습니다
```

#### 💡 이게 왜 중요한가요?

**"아무 일도 안 일어남"과 "도구가 못 본 것"은 완전히 다르기 때문입니다.**

| 화면에 나오는 말 | 뜻 | 여러분이 할 일 |
| :--- | :--- | :--- |
| `상태 변화 없음` | 진짜로 상태를 안 바꿉니다 | 그냥 그런 버튼입니다 |
| 🟠 `prop 으로 전달받은 함수` | 부모가 넘겨준 함수입니다 | **부모 코드도 같이 붙여넣어 보세요** |
| 🟠 `useXxx 에서 받아옴` | 그 훅이 다른 파일에 있습니다 | **그 훅 파일도 같이 붙여넣어 보세요** |

주황 점선을 보면 "아, 코드를 더 붙여넣으면 더 보이겠구나" 하고 알 수 있습니다.

### 🪝 커스텀 훅도 따라갑니다

로직을 `useSearch` 같은 커스텀 훅으로 빼는 건 **아주 좋은 습관**입니다. 그리고 그 훅이 **같은 파일 안에 있으면** Code Bunseki 가 끝까지 따라갑니다.

이때 **훅 안에서 일어나는 일들은 점선 상자로 묶여서** 나옵니다.

```
 ① 이벤트
    <input onChange>
         ↓
 ┌ 🪝 useSearch — 이 구간의 상태와 Effect 는 이 훅 안에 있습니다   L3 ┐
 │  ② 상태 변경   setQuery()  →  query                            │
 │  ③ Effect 실행  useEffect 재실행                                │
 │  ④ 함수 호출    search()                                        │
 │  ⑤ 대기         ⏳ 응답 대기                                     │
 │  ⑥ 상태 변경    setHits()  →  hits                              │
 └──────────────────────────────────────────────────────────────┘
         ↓
 ⑦ 화면 갱신   리렌더
```

**상자 = "여기서부터 여기까지는 저 훅이 하는 일"** 입니다.
상자가 닫히는 자리가 **흐름이 훅에서 나오는 자리**입니다.
상자 머리말을 클릭하면 그 훅 선언 위치로 이동합니다.

> 💡 **이름이 달라 보여도 당황하지 마세요.**
> `const [on, toggle] = useToggle()` 처럼 이름을 바꿔 받았다면
> `toggle() → on (useToggle 안에서는 setOn() 입니다)` 라고 **양쪽 이름을 다 알려 줍니다.**

### ↩ 관문 — "여기서 되돌아 나갈 수 있어요"

코드에 이런 게 있으면:

```jsx
useEffect(() => {
  if (!open) return      // ← open 이 false 면 여기서 끝!
  fetchData()
}, [open])
```

동작 탭은 **끊긴 테두리에 `↩` 가 붙은 칸**으로 그 자리를 보여 줍니다.

```
 ② Effect 실행   useEffect 재실행
         ↓
 ↩  !open 면 중단
    이 조건이 참이면 아래 단계는 실행되지 않습니다
         ↓
 ③ 함수 호출     fetchData()
```

**"항상 여기까지 간다" 가 아니라 "여기서 멈출 수도 있다"** 는 뜻입니다.
`↩` 는 **번호를 먹지 않습니다** — 그래서 ①②③ 번호가 건너뛰지 않습니다.

> `if (!id) throw ...` 처럼 오류를 던지고 끝나면 `!id 면 **오류**` 라고 적힙니다.

### ⚠️ 그래도 기억할 것 — 이건 "될 수 있다" 지 "반드시 된다" 가 아닙니다

Code Bunseki 는 **코드를 읽을 뿐 실행해 보지는 않습니다.**
`if` 안에 실제로 어떤 값이 들어올지는 돌려 봐야 아는 일입니다.

그래서 **갈 수 있는 길을 보여 주고, 되돌아 나갈 수 있는 자리(`↩`)를 표시**하는 쪽을 택했습니다.
`↩` 아래 단계들은 **"조건이 거짓일 때 가는 길"** 입니다.

---

### 📋 동작 탭 아래에 있는 두 개의 점검 코너

이벤트 순서표를 지나 **더 아래로 스크롤하면** 코너가 두 개 더 있습니다.
여기는 **문제가 있을 때만** 내용이 나옵니다. 비어 있으면 좋은 겁니다!

#### ⏱ 타이밍 · deps 점검

제목 옆에 `위험 2` `deps 빠짐 1` 같은 숫자가 붙습니다.

| 표시 | 뜻 | 쉽게 말하면 |
| :--- | :--- | :--- |
| ⚠️ **언마운트 후 setState 위험** | 답이 오기 전에 화면이 사라지면, 없는 화면을 고치려 듭니다 | 정리(cleanup)에서 취소하거나 `if (!alive) return` 을 두세요 |
| ⚠️ **`[userId]` 빠짐?** | Effect 가 쓰는 값이 `deps` 목록에 없습니다 | 옛날 값을 붙잡고 있을 수 있어요 → `deps` 에 넣으세요 |
| 🛡 **가드됨** | 이미 안전장치가 있습니다 | 잘 쓴 코드입니다 👍 |
| **정리 없음** | `useEffect` 가 정리 함수를 안 돌려줍니다 | 타이머·구독을 썼다면 정리해 주세요 |

#### 🔗 Effect 사이 관계

`useEffect` 여러 개가 서로 얽히는 모양을 카드로 보여 줍니다.

| 배지 | 뜻 |
| :--- | :--- |
| **[무한 루프]** | A 가 상태를 바꾸면 A 가 또 실행됩니다 → **화면이 계속 깜빡일 수 있어요** |
| **[경합]** | 같은 상태를 Effect 둘이 바꿉니다 → **늦게 온 옛 답이 새 값을 덮을 수 있어요** |
| **[연쇄]** | 도미노처럼 이어집니다 (정보 표시일 뿐, 나쁜 건 아닙니다) |

> 💡 **아무것도 안 나오면 성공입니다.** 이 코너들은 잔소리를 하려고 있는 게 아니라
> **놓치기 쉬운 것만 골라서** 알려 주려고 있습니다.

---

## 6. 🧹 왕초보도 따라 하는 3분 코드 청소(리팩토링) 법

### 💡 청소 1단계: 뚱뚱한 막대 상자 쪼개기
- 메트릭 탭에서 제일 긴 막대(100줄 이상)를 발견했다면, 컴포넌트 안에서 **버튼이나 리스트 항목 하나만 따로 잘라내어 새 컴포넌트로 분리**해 보세요.

### 💡 청소 2단계: 비슷하게 생긴 코드 합치기
- 똑같이 생긴 버튼이나 카드 코드가 반복된다면, 하나의 컴포넌트로 만들어 재사용하세요.

### 💡 청소 3단계: 노란색 헬퍼(Helper) 함수 다듬기
- **혼자 떨어진 헬퍼 함수 지우기**: 플로우 탭 맨 아래에 화살표 없이 덩그러니 서 있는 노란 상자는 **안 쓰는 코드**일 수 있습니다.
- **길고 복잡한 헬퍼는 밖으로**: `utils.js` 같은 별도 파일로 빼면 훨씬 깔끔해집니다.

### 💡 청소 4단계: **동작 탭으로 상태 연쇄 끊기** ⭐ 새로 추가
동작 탭에서 이런 모양이 보이면 위험 신호입니다:

```
 ② setLoading()  →  loading
 ③ useEffect 재실행  (deps [loading])
 ④ setData()  →  data
 ⑤ useEffect 재실행  (deps [data])     ← 또!
 ⑥ setLoading()  →  loading            ← 처음으로 돌아감!
```

상태가 Effect 를 깨우고, 그 Effect 가 또 상태를 바꾸는 **도미노**입니다. 이러면 화면이 여러 번 깜빡이거나 무한 루프에 빠질 수 있습니다.

**해결 방법**: 계산으로 구할 수 있는 값은 상태로 만들지 말고, 렌더링할 때 그냥 계산하세요.

```jsx
// ❌ 상태 도미노
const [total, setTotal] = useState(0)
useEffect(() => { setTotal(items.length) }, [items])

// ✅ 그냥 계산
const total = items.length
```

---

## 7. ❓ FAQ: 잘린 코드도 검진되나요?

### Q. 코드가 중간에 잘렸는데 분석되나요?

**❌ 잘린 코드는 제대로 분석되지 않습니다. 다섯 탭 모두요.**

괄호나 태그가 닫히지 않으면 코드를 읽다가 **거기서 멈춥니다.** 그러면 이렇게 됩니다:

**다행히 화면이 그 사실을 분명히 알려 줍니다.** 다섯 탭 모두 이렇게 나옵니다:

```
문법 오류로 코드를 읽지 못했습니다
예상하지 못한 토큰이 있습니다.
L11

괄호나 태그가 닫히지 않으면 다섯 탭 모두 분석하지 못합니다.
컴포넌트를 통째로(여는 function 부터 닫는 } 까지) 붙여넣어 주세요.
```

| 탭 | 잘린 코드에서 보이는 것 |
| :--- | :--- |
| 🎨 하이라이트 | **안내 카드 + 코드 자체** (어디가 잘렸는지 눈으로 찾으라고 코드는 보여 줍니다) |
| 🗺️ 구조맵 / 📊 메트릭 / 🔄 플로우 | **안내 카드** |
| ⚡ 동작 | **안내 카드** (오류 위치 `L11` 포함) |

아래 상태바의 숫자도 `함수: —` 처럼 **줄표**로 나옵니다.

### 🚨 `0` 과 `—` 의 차이를 기억하세요

- **`0`** = "세어 봤더니 없다" ← 사실입니다
- **`—`** = "못 셌다" ← 코드가 깨져서 세지 못한 것입니다

예전에는 이 둘이 똑같이 `0` 으로 보여서, 멀쩡한 파일인데 "아무것도 없는 파일" 로
오해할 수 있었습니다. 지금은 구분됩니다.

### ✅ 해결 방법은 간단합니다

**괄호와 태그를 닫아 주세요.** 컴포넌트 하나를 통째로(여는 `function` 부터 닫는 `}` 까지)
붙여넣으면 다섯 탭 모두 정상 동작합니다.

일부만 보고 싶다면 이렇게 감싸 주면 됩니다:

```jsx
export default function 임시() {
  // 여기에 보고 싶은 코드 조각
  return null
}
```

---

### Q. 동작 탭에 "상태 변화 없음"만 잔뜩 나와요!

두 가지 경우입니다.

**1. 주황 점선이 함께 보인다면** → 코드를 더 붙여넣으면 됩니다. 부모 컴포넌트나 커스텀 훅 파일을 같이 넣어 보세요.

**2. 정말 아무 표시도 없다면** → 그 버튼들이 실제로 상태를 바꾸지 않는 겁니다. 예를 들어 부모에게 알림만 보내는 버튼이 그렇습니다.

---

### Q. 파일을 여러 개 붙여넣어도 되나요?

**네, 오히려 권장합니다!** 컴포넌트와 그 컴포넌트가 쓰는 커스텀 훅을 **한 번에 붙여넣으면** 연쇄가 끊기지 않고 끝까지 이어집니다.

---

### Q. TypeScript(.tsx) 도 되나요?

**됩니다.** `useState<string>('')` 같은 제네릭, 타입 주석, `interface` 모두 정상적으로 읽습니다. 위쪽 언어 선택에서 `TSX / TypeScript` 를 고르면 색칠도 맞게 됩니다.

---

## 8. 🚨 꼭 알아 둘 주의사항 8가지

### 1. 붙여넣은 코드는 저장되지 않습니다
새로고침하면 사라집니다. 기억되는 건 **밝게/어둡게 설정 하나뿐**입니다.
길게 작업할 코드는 원본 파일을 따로 두세요.

### 2. 이 도구는 코드를 **읽을 뿐, 실행하지 않습니다**
`if` 안에 어떤 값이 들어올지는 돌려 봐야 아는 일입니다.
그래서 **갈 수 있는 길**을 보여 주고, 되돌아 나갈 수 있는 자리는 `↩` 로 표시합니다.
👉 **"이렇게 될 수 있다" 이지 "반드시 이렇게 된다" 가 아닙니다.**

### 3. **한 파일만** 봅니다
`import` 로 가져온 다른 파일은 못 봅니다. 대신 **못 보는 지점을 숨기지 않고 표시**합니다
(🟠 주황 점선). 컴포넌트와 그 컴포넌트가 쓰는 커스텀 훅을 **같이 붙여넣으면** 훨씬 잘 보입니다.

### 4. **문법이 완전해야** 합니다 — 다섯 탭 모두
괄호나 태그가 안 닫힌 코드는 모든 탭이 못 읽습니다.
다만 **왜 못 읽었는지 화면이 알려 주고**, 숫자도 `0` 이 아니라 `—` 로 나옵니다.
안내 카드가 보이면 **괄호·태그를 닫아 주세요.** ([7장](#7--faq-잘린-코드도-검진되나요))

### 5. 경고가 없다고 **버그가 없다는 뜻은 아닙니다**
이 도구는 **헛경보를 안 내는 쪽**을 택했습니다.
애매하면 조용히 넘어갑니다. 그러니 **경고가 뜨면 진지하게 보되, 안 떴다고 안심하지는 마세요.**

### 6. 반대로, **뜬 경고는 대체로 진짜입니다**
`deps 빠짐` 은 ESLint 의 `exhaustive-deps` 와 같은 지점을 가리킵니다.
`무한 루프` 카드가 떴다면 실제로 화면이 깜빡일 가능성이 높습니다.

### 7. 버튼 핸들러에는 **위험 배지가 안 붙습니다**
`onClick={async () => { await 저장(); setDone(true) }}` 같은 코드는 **기다리는 구간(⏳)은 보여 주지만
빨간 경고는 달지 않습니다.** 이 모양은 거의 모든 React 코드에 있어서, 경고로 만들면 잔소리만 늘기 때문입니다.
(React 18 도 같은 이유로 이 경고를 없앴습니다.)

### 8. 아주 큰 파일은 **결과가 매우 깁니다**
2500줄쯤 되는 파일도 1초 안에 분석되지만, **동작 탭 스크롤이 아주 길어집니다.**
큰 파일은 **보고 싶은 컴포넌트만 잘라서** 붙여넣는 편이 읽기 좋습니다.

---

## 🎁 마지막으로 — 이 도구를 가장 잘 쓰는 법

**"내가 짠 코드가 맞는지"** 를 확인하는 도구가 아니라,
**"이 코드가 실제로 어떻게 도는지"** 를 눈으로 보는 도구라고 생각하세요.

1. 남이 짠 컴포넌트를 처음 열었을 때 → **⚡ 동작 탭부터** 보세요. 버튼 하나 눌러 보면 전체가 잡힙니다.
2. "왜 두 번 렌더되지?" 싶을 때 → **🔗 Effect 사이 관계** 코너를 보세요.
3. "이 `useEffect` 왜 자꾸 도는 거야?" 싶을 때 → **⏱ 타이밍 · deps 점검** 을 보세요.
4. 리팩토링 전후로 한 번씩 돌려 보면 **정말 단순해졌는지** 눈으로 확인됩니다.
